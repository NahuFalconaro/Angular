import { FiltroCompletarPipe } from './filtro-completar.pipe';

describe('FiltroCompletarPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletarPipe();
    expect(pipe).toBeTruthy();
  });
});
